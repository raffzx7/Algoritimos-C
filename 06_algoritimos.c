#include <stdio.h>

int main() {

    int n1, area;

    printf("insira o raio do circulo: ");
    scanf("%d", &n1);

    area = (n1 * 2) * 3,14;

    printf("a area do circulo é: %d\n", area);

    return 0;
}
